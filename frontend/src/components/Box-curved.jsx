import React from "react";
import '../css/Box.css';

const Box_curved = ({name}) => {
    return(
        <div className="box">
            <p>{name}</p>
        </div>
    );
}

export default Box_curved;